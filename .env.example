# Copy to .env (never commit .env). Credentials are supplied externally.
WEBVAL_AUTH__USERNAME=your-username
WEBVAL_AUTH__PASSWORD=your-password

# Optional overrides
# WEBVAL_SITE__BASE_URL=https://usim.preprod.sbx.us.pluvicto.com/
# WEBVAL_BROWSER__HEADLESS=true
